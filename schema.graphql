type Query {
  allBooks: [Book]
  getBook(id: ID!): Book
  hello: String
}

type Mutation {
  addBook(title: String!, author: String!): Book
  removeBook(id: ID!): Book
}

type Book {
  id: ID!
  title: String
  author: String
}
